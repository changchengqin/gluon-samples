name: Linux

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      
      - name: current dir display 
        run: pwd
        
      - name: change dir
        run: java --version;$GRAALVM_11_ROOT
        
      - name: current dir display 
        run: ls
        
      - name: maven version display
        run: mvn --version
